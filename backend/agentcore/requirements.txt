# Pinned to a verified conflict-free set to avoid pip dependency backtracking.
# The constraint window is narrow: bedrock-agentcore needs boto3>=1.40.52 while
# aioboto3 15.5.0 (aiobotocore 2.25.1) needs boto3<1.40.62 — so boto3 must be
# 1.40.52–1.40.61. strands-agents 1.55.x moved BidiNovaSonicModel, so it stays at 1.54.0.
# Do NOT loosen these without re-checking the overlap.
strands-agents[bidi,otel]==1.54.0
bedrock-agentcore==1.3.0
boto3==1.40.61
botocore==1.40.61
aioboto3==15.5.0
pyaudio==0.2.14
jinja2==3.1.6
